Makefile creates the objects file using a 
objects variable. This is less error-prone, given
that that we only have to list the objects files in
the objects variable instead of both the prerequisites and
recipe

